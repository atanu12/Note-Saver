import React from 'react'
// import { Alert } from '@mui/material';

const ErrorHandler = ({varient = 'info', children}) => {
    return (
        <div>
            {/* <Alert variant={varient} severity={varient}>
  <strong>{children}</strong>
</Alert> */}
        </div>
    )
}

export default ErrorHandler
